export interface EscapeRoom {
    name: string;
    shortDescription: string;
    images: Array<string>;
    recommendation1: string;
    recommendation2: string;
    tags: Array<string>;
    description: string;
    price: number;
};